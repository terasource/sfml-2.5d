#include "texture.hpp"


/*

template <typename T>
void texture<T>::assign_sprite_texture(sf::Sprite& sprite){

};

this probably breaks the srp and should not be in this scope, 
but im not sure whether i should seperate the sprite and texture or not.
i just cant find a good valiable reason for to do one.
with seperating it implements srp correctly as it seems so okay but,
without separating they are combined and revelant concepts are handled together this also seems okay to me idk.

*/
